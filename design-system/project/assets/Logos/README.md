Logos clients individuels en noir et blanc, extraits du dossier `Logo Clients collectif/NB` du Drive — c'est la matière première qui a servi à composer le visuel unique `assets/Marque/partenaires.png` (voir sa note d'usage) : le site déployé n'affiche jamais ces logos séparément, donc ne pas les utiliser dans la section Brand Collaborations, qui reste le composite. Utile en revanche pour une nouvelle landing page qui a besoin d'un logo client isolé (étude de cas, page projet dédiée) : 22 fichiers, chacun le traitement noir et blanc tel que fourni par le client ou converti côté agence — ne pas les repasser en couleur ni les recolorer, garder le rendu monochrome d'origine. Deux fichiers Quiksilver coexistent (`quiksilver-logo-1.png`, `quiksilver-logo-2.png`) car les sources Drive proposent deux déclinaisons non identiques — vérifier laquelle convient avant usage plutôt que d'en choisir une au hasard. Le dossier `Couleurs` équivalent (mêmes clients, version couleur) n'a pas encore été importé dans ce système — à ajouter si un besoin précis se présente.
