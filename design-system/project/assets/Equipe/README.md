Photos réelles des membres de la grille Freelances (`/about`), vérifiées face au live (noms et fichiers correspondent exactement aux `alt` des balises `<img>` du site). Pas encore recadrées : sur le site elles sont affichées rondes et désaturées en noir et blanc via CSS (`radius-circle`, `filter: grayscale`), pas retravaillées en amont — reprendre ce traitement en CSS plutôt que fournir des fichiers déjà recadrés, pour rester modifiable. `google.png` est un membre nommé "Google" dans le jeu de données du site (à vérifier auprès de Victoire si c'est un vrai nom d'équipe ou un badge). Valentine Rolaz manque encore ici (pas fournie côté Drive au moment de cet import) alors qu'elle apparaît déjà sur le site live — à ajouter dès qu'elle est disponible plutôt que de laisser un membre sans photo dans un nouveau composant.
